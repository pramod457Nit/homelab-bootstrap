name: CI

on:
  push:
  pull_request:

jobs:
  bash-checks:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Bash syntax check
        run: bash tests/syntax.sh

      - name: Basic secret scan
        run: bash scripts/secret-scan-basic.sh
